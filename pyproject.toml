[project]
name = "aifilm-p00-contracts"
version = "0.1.0.dev14"
description = "Partial Phase00 implementation: aligns reviewed DIRECT probe with explicit network-failure handling for configured proxy contexts; remaining harness/factory work pending"
requires-python = ">=3.11"
dependencies = []

[tool.aifilm-p00]
status = "IMPLEMENTATION_PARTIAL"
native-backend-available = false
code-review-status = "NOT_PERFORMED"
