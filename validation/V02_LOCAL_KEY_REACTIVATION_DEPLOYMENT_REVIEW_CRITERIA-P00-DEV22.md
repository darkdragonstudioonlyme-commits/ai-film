# V02 dev22 local-key reactivation deployment review criteria

Review must bind exact canonical source SHA/CI and local receipt hashes; independently re-verify 20 deployed file hashes, deployed trust SHA, live private-derived-public key parity without exposing private bytes, preserved local identity-context SHA, watcher state and blocked live gates; confirm tooling bytes did not change in the deployment-state commit; and confirm zero native execution.
