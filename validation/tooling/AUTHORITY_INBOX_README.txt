AI-FILM Phase00 V02 local-operator authority intake — dev22

This inbox is same-trust-domain local authority by owner Choice B. It is not independent/external approval and must never be described that way.
Do not rename approval-envelope.template.json to approval-envelope.json until the local operator has intentionally approved exact candidate 6f895394-e0b4-5434-bebc-79ee4e576282 and the complete signed object graph has been generated.

A valid intake requires hash-addressed JSON objects in objects/<sha256>.json plus approval-envelope.json binding candidate-binding SHA 4aaf09ec2ef8618a5680e147cd2eeac695f940d45ae5cb0446c7b7e5c2483384, exact dev22 source/build/test/contract identities, local authority model, registration/fixture containment, all 86 reviewed procedures, <=24h suite validity, execution-plan authorization and role pins.

Registration and every native fixture must use controller_external=false. disposable=true, no_real_credentials=true, and no_production_mappings=true remain mandatory.

The Ed25519 private key is intentionally permitted on this WSL host under Choice B, but only after the local-authority tooling/trust schema is independently reviewed/audited. Keep the private key outside Git and mode 0600. The committed public trust anchor is hash-pinned. approval-envelope.sig.json signs exact raw approval-envelope.json bytes and proves local key possession/integrity only.

No passwords, raw MachineGuid, unrelated credentials, or private signing key bytes belong in this inbox or Git.
