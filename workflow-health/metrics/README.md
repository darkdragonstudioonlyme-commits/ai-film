# Workflow-health metric snapshots

Immutable snapshots establish comparable populations/windows for continuous-improvement claims. A snapshot records what can be measured and explicitly marks unavailable trend metrics as `NOT_ENOUGH_DATA`; activity counts are never substituted for improvement.

Future snapshots compare against a named prior snapshot using the same metric definition or declare a metric-version change.
