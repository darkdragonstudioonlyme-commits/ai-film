# Documentation R9 V39 — Holistic Audit Criteria

A holistic audit may PASS only if it evaluates the same exact design SHA as R7 detailed review and confirms:

1. R7 review is PASS with zero findings and binds the exact V39 design SHA.
2. Product implementation/native test oracle remain unchanged; V39 is documentation/control-plane/lifecycle reconciliation only.
3. V02 remains external-authority-only and all native execution counts remain zero.
4. Ten supervised timers and the 58-file control state are supported by durable validation evidence and exclude protected authority, protected identity and credentials.
5. Full DR rehearsal restores the current recovery schema in a disposable root and reconstructs exact dev21; weekly fail-closed campaign rejects all eight planned corrupted/stale/unsafe cases using actual verifier modules.
6. The old-schema 44-file rehearsal failure is preserved and resolved through producer migration rather than checker weakening.
7. Health/recovery integrity ordering uses explicit `Wants+After` dependencies and supervised jobs/timeouts remain fail-closed.
8. V02 staging preflight is read-only and cannot substitute for external protected authority.
9. Private Google Drive evidence pins stable exact identity only; rotating state remains host-side verified and binary off-host payload is absent.
10. Learning 005 effectiveness is supported by the exact V39 target and does not rely only on the state-version number.
11. Learning 006 is activated only through R7/A7 promotion and remains pending until V41.
12. Aggregate truth is backlog=0, unresolved ineffective=0, pending=1, overdue=0, historical ineffective=3.
13. Lifecycle/adversarial/governance/docs/audit/continuity/runtime-state executable checks PASS on the exact target.
14. Promotion is verdict-only and post-promotion CI is mandatory.

Any mismatch between review target, audit target, lifecycle state, recovery evidence, validation head or native boundary is an audit failure.
