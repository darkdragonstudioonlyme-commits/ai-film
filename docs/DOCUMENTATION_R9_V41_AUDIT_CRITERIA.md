# Documentation R9 V41 — Holistic Audit Criteria

A9 audit may PASS only if R9 detailed review PASS exists and binds the same exact V41 design SHA, and all of the following hold:

1. Canonical ownership remains one active run and one lifecycle register.
2. Product identity and native boundary are unchanged: V02 external-authority-only, LAB not run, 86 cases NOT_RUN, qualification/SITE/HOST_READY absent.
3. Validation evidence supports execution freshness, retention overflow tests, resource cgroup binding and producer-first recovery refresh.
4. Timer enabled/active state is not treated as sufficient when service execution freshness is stale.
5. Retention prune behavior is fail-closed and chain-preserving; no live evidence was deleted to force a target count.
6. Resource containment evidence uses safe kernel-binding verification and does not rely on destructive host exhaustion.
7. Recovery consumers were not weakened for rotating/older payloads; producers were regenerated before success was claimed.
8. Stable identity is separated from rotating recovery samples in canonical state and off-host metadata semantics.
9. Learning 006 effectiveness is supported by its due V41 gate, real recovery-state transition and exact executable/CI evidence; no measurement is inferred from state number alone.
10. Learning aggregates match canonical state and there is no overdue/pending effectiveness debt after successful V41 measurement.
11. R9/A9 promotion is verdict-only and post-promotion CI is mandatory.

Any mismatch, mixed target, weakened predicate, unsupported stable-identity claim or native-state advancement requires audit FAIL.
