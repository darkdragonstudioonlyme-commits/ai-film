# V72 — shared workflow, seven-goal completion contract
RELEASE_ID: DOCSYS-V2-R9
BASE_MAIN_COMMIT: 37d8e5049982a743d77f8ecaba358f2fc5d91b44
PREDECESSOR_CANDIDATE: 5dbcaa5efbd27ee7ea4169328a38ef23fbb67b66
PRESERVED_SOURCE: 86bb64938a136e3f8d6cfd0266685a01cb832b77
FINAL_REVIEW_ID: DOC-V2-R9-REVIEW-038
FINAL_AUDIT_ID: DOC-V2-R9-AUDIT-038
STATUS: CANDIDATE_REQUIRES_CROSS_MODEL_ACCEPTANCE

## Scope and acceptance
This completes the existing V72 correction; it does not open another design release. The combined main-to-candidate delta includes the unpromoted V70/V71 work. Read all 21 active root documents, current collaboration/transport design, changed guards/tests and their referenced evidence. Historical archives are inventoried separately, not claimed as freshly validated.

| Goal | Correction | Acceptance evidence and boundary |
|---|---|---|
| Shared learning | One register; derive terminal operative lessons and predecessor warnings; current invalidations block use | Selection/cycle/stale-context negatives; later real transfer is still required for effectiveness |
| Current state | Task, actor, author, role, profile, next and return must agree | Actor/role/cursor tests; no inference from accepted product readiness |
| Shared meaning | Exact task/context/acceptance and non-author review | Both actors inspect the same artifacts; disagreement resolved by evidence |
| Design conformance | Trace producer/consumer, temporal/hash/lock dependencies; scoped applicability hold | Counterexample and interruption witness before affected implementation |
| Testing/review | Business oracle precedes code; static/host/native evidence separated | Strict JSON, coverage, unresolved-high and self-report rejection |
| Lifecycle/size | Compact memory/state; preserve immutable evidence and safety policy | Fixed-population byte comparison; no automatic historical deletion |
| Automatic work | Bounded foreground delivery; separate profile qualification | Real executor/context/permission/replay tests before activation |

## Findings addressed
V71 allowed actor drift and treated historical ACTIVE predecessors as current retrieval candidates. V72 fixes both. Detached technical review of the first V72 WIP found mode/role mismatch and non-JSON result acceptance; corrections at 11357a9 were retested. Three missing delivery records and CI wiring are completed by this transaction. Setup connectivity is historical evidence, not full acceptance or sandbox proof.

## Frozen acceptance and promotion
Run all 19 existing portable/local commands on a clean exact commit, including the eight suites (244 cases at the completion baseline). Keep original source, package, 18-entry learning register, historic snapshots and native outcomes unchanged. A failing guard blocks acceptance; tests alone never authenticate a worker's claims.

Claude's first review must receive complete-for-scope artifact bytes, not an author summary. Record manifest hashes, exact candidate, model, raw response identity, omissions, findings and learning usage. A separate audit pass consumes the corrected final target. Host-run tests are attributed to the host. Findings are corrected by the author and re-reviewed, never patched in a review checkout.

After freeze the permitted append set is reviews/DOCUMENTATION_SYSTEM_R9_REVIEW_R38_PASS.md, reviews/DOCUMENTATION_SYSTEM_R9_AUDIT_R38_PASS.md, test-governance/TEST_REVIEW-DOCSYS-CURRENT-WORK-007.md, test-governance/TEST_REVIEW-DOCSYS-DUAL-AI-008.md, and test-governance/TEST_REVIEW-DOCSYS-SHARED-WORKFLOW-009.md. Each record must bind the actual author target and evidence; reserved names do not imply PASS. Any semantic change requires a new author target and affected review/audit. Main promotion preserves the exact audited tree plus this append set and requires a fresh expected-parent check.

## Completion levels and remaining work
Policy acceptance permits bounded executor implementation and qualification; it is not live activation. The next runtime work is a foreground, tool-less TEXT_REVIEW bridge with exact context loading, no raw shell in tasks, persistent idempotency, crash/timeout reconciliation and a host-verified result. WSL_IMPLEMENT remains disabled until its separate filesystem/network/command containment is actually qualified. Product authority feasibility and affected test review still precede dev23; all 86 native cases remain NOT_RUN.

The known feasibility supplement is a proposal requiring product DESIGN_REVIEW. This documentation acceptance neither endorses its unresolved protocol choices nor issues native authority. Knowledge effectiveness remains IMPROVEMENT_NOT_PROVEN; future task receipts must include failures and exclusions. Do not fabricate prior INTENT or turn this policy's activation into its own success sample.

## Evidence
The baseline audit is under /home/dragon/ai-film-dev/run-evidence/shared-workflow-seven-goals-20260922. METRICS-V72-SEVEN-GOALS-20260922.json retains its exact population and missing-data limits. Completion-run logs are stored separately and bind their own exact candidate. Review assurance is CROSS_MODEL_PROCEDURAL_REVIEW only for actual Claude responses; local technical checks remain host evidence.

## Cross-model correction disposition
Scope-A review of 1396d08 returned findings. SCOPEA-001 exposed projection ambiguity: the intended PROMOTED content is now explicitly distinguished from actual ref/verdict-based authority in both MD and JSON, preserving the existing exact-tree rule. SCOPEA-002 is clarified by a revision name without a verdict ordinal; final verdict IDs remain the sole authority. SCOPEA-003 proposed editing immutable discovery records; that fix is rejected because it violates the two-layer lifecycle. The canonical policy now explicitly covers post-R9 snapshot labels and requires current-status framing without rewriting old evidence. The reviewer also reported reads of unsupplied lesson bodies and classified a pending terminal lesson as historical; those claims are not accepted as learning transfer. A new regression rejects unsupplied read IDs. Host challenge additionally found an unusable terminal successor did not flag its predecessor and AUTHOR could use a text-review profile; four regressions close these model gaps. The intended next cursor now names bridge implementation, avoiding a post-promotion review-of-itself loop.

## Consolidated acceptance-boundary check
V72-R-02 asserted there was no closed acceptance enum anywhere; the existing dual-ai validator does have one, but the small correction packet omitted that dependency. The shared helper now validates it independently and requires the three disclaimer/scope fields for every PROMOTED projection, not only NOT_RUN. V72-R-03 parity is enforced for both PROJECTION_SEMANTICS and ACCEPTANCE_RULE. One truth table, not another policy family, governs missing/invalid states and MD/JSON drift. Host review also removed a fixed 12/6 assertion against the mutable real learning population; current integration expectations are derived and a valid-growth regression prevents future lessons from breaking the test for incidental counts. Historical 12/6 observations remain immutable measurements, not forever-required population sizes. These corrections reuse existing CONTROL/ADVERSARIAL/authority lessons; no EFFECTIVE transition is claimed.
