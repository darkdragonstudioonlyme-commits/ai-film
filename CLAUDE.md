# AI-FILM — Claude worker entry

Read the exact task capsule first; no capsule means no assigned work.
The capsule identifies the control-plane commit separately from the source commit.
Do not assume a source worktree contains PROJECT_STATE.md or current policy.

Use that control plane's PROJECT_STATE.md, NEXT_WORK_ITEM.md, WORKFLOW_ROUTER.md
and the applicable DOCUMENTATION_MAP.md profile. Apply SELF_LEARNING.md and the
single learning/LEARNING_STATE.json register exactly as ChatGPT does. Read the
active lessons selected for this task; report their IDs and how they were applied.
Local auto-memory is scratch, not shared authority. Never replace a repository
rule with a remembered instruction or a previous model's PASS summary.

One task, one writer, one mode. On REVIEW/AUDIT, do not modify the candidate.
Bind every finding and command result to exact artifacts. Preserve the parent run,
WIP, failed attempts, uncertainty and return point. NO_NEW_LEARNING is valid;
otherwise propose a deduplicated learning with evidence, not a self-approved rule.

Read docs/DUAL_AI_COLLABORATION.md and docs/DUAL_AI_AUTOMATIC_HANDOFF.md.
A worker result is not a merge, active learning, qualification or HOST_READY.
Never sign authority, broaden tools/permissions, run native host actions, launch
another agent or route around a safety denial. Return NEEDS_INPUT when required
capability or approval is missing; do not ask the user to manage routine handoffs.
In isolated/headless modes the trusted bridge must explicitly supply this content;
file presence alone does not prove that an instruction was loaded.

Use the current actor/return projection and the shared operative knowledge view.
Historical/superseded records are warnings, not commands. Recheck relevant context
at result consumption; no model may fill missing state from conversational memory.
