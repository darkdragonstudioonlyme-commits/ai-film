"""Windows adapters. Importing this package never loads DLLs or launches processes.

Concrete adapters are Windows-only; workspace tests exercise parsers/protocols with
explicit doubles. Native qualification is a separate, not-yet-run activity.
"""
